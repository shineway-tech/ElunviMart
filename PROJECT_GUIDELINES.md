# Elunvi Mart 项目规范

## 1. 项目定位

Elunvi Mart 是一个基于 Electron 的拼多多商家百亿补贴状态监控客户端，面向需要管理多个拼多多商家账号的运营人员。

项目的核心流程是：

1. 通过独立浏览器会话登录拼多多商家后台。
2. 获取账号参与的营销活动商品快照。
3. 将商品状态保存到本地，并识别疑似掉标和已掉标商品。
4. 按配置的渠道发送提醒。

### 项目限定

- 只支持 Electron 桌面客户端，不建设 Web 服务端和移动端。
- 一个客户端最多管理 10 个商家账号。
- 每个商家账号必须使用独立的持久化浏览器分区，账号之间不得共享登录会话。
- 商品同步适配器必须返回完整商品快照；缺失商品默认视为不再参与活动。
- 本地业务数据统一存储在 SQLite 数据库中，不新增 JSON 数据库实现；旧版本 JSON 数据只用于一次性迁移。
- 拼多多接口尚未完成适配时，客户端必须继续展示本地缓存，并明确标记数据来源。
- 不在客户端保存拼多多账号密码；登录必须通过拼多多商家后台页面完成。
- Webhook、加签密钥等敏感配置必须通过 Electron `safeStorage` 加密后再落盘。
- 不绕过拼多多权限、验证码、风控或接口授权，不猜测和固化未经确认的私有接口。
- 当前提醒渠道限定为桌面通知、企业微信机器人和钉钉机器人。

### 暂不包含的范围

- 自动填写或代管拼多多账号密码。
- 自动下单、改价、库存操作或其他交易操作。
- 多用户协作、云端账号同步和远程管理。
- 服务端数据库和远程任务队列。
- 未经验证的拼多多内部 API 逆向适配。

## 2. 模块划分

模块必须保持单一职责，通过明确的函数参数、返回值或 IPC 接口通信。业务模块不得直接操作另一个模块的内部状态。

### 主进程：`src/main/`

#### `main.js`

负责 Electron 生命周期和应用编排：

- 创建主窗口和登录窗口。
- 注册 IPC 处理器。
- 管理账号登录窗口和浏览器分区。
- 初始化 `JsonStore`、`PddActivityAdapter` 和 `MonitorScheduler`。
- 编排同步、状态更新和通知流程。

`main.js` 可以组合其他模块，但不应承载商品状态规则、通知请求构造或数据文件读写细节。

#### `pdd-adapter.js`

负责拼多多后台活动商品数据的获取和字段映射：

- 接收账号信息和登录会话。
- 返回完整商品快照。
- 将外部响应转换为项目内部商品模型。
- 对接口未配置、响应格式错误和认证失败给出明确错误。

该模块不得写入 Store，不得触发通知，不得操作渲染层。

#### `product-monitor.js`

负责纯业务规则：

- 校验和标准化商品数据。
- 合并上一份快照和当前快照。
- 判断商品是否变为 `suspected` 或 `lost`。
- 构造状态变化提醒内容。

该模块应尽量保持纯函数，不依赖 Electron、文件系统、网络和 DOM。

#### `scheduler.js`

负责检测任务的时间安排：

- 根据最短和最长间隔计算下一次执行时间。
- 管理定时器的启动、重排和停止。
- 调用注入的任务函数。

调度器不负责账号查询、商品同步或通知发送。

#### `store.js`

负责本地 SQLite 数据库的读取和持久化：

- 账号数据。
- 商品缓存。
- 监控设置。
- 数据表初始化、索引、事务和旧版 JSON 数据迁移。

Store 不得依赖渲染层，也不得包含 Electron 窗口逻辑。对外返回的数据应避免暴露内部可变引用。账号、商品和设置必须通过 Store 访问，不得在业务模块中直接拼接 SQL。

#### `notifier.js`

负责提醒渠道：

- 校验企业微信和钉钉 Webhook 地址。
- 发送桌面通知。
- 发送企业微信和钉钉机器人消息。
- 处理超时、HTTP 错误和渠道返回错误。

通知模块接收已经构造好的消息，不负责判断商品状态。

### 渲染进程：`src/renderer/`

#### `preload.js`

只负责暴露经过限制的安全 API：

- 账号操作。
- 商品查询和同步。
- 设置读取和保存。
- 通知测试。

不得向渲染层暴露完整 `ipcRenderer`、Node.js 文件系统或任意命令执行能力。

#### `app.js`

负责页面状态和交互：

- 加载和渲染账号、商品、设置。
- 处理表单、弹窗、筛选和操作反馈。
- 调用 `window.pddMonitor` 提供的 API。

渲染层不得直接访问文件系统、网络接口或 Electron 主进程对象。商品状态判断和敏感配置处理必须留在主进程。

#### `index.html` 与 `styles.css`

- `index.html` 负责页面结构和可访问性标记。
- `styles.css` 负责布局、颜色、状态样式和响应式表现。
- 交互逻辑不写入 HTML 内联脚本。
- 颜色、间距和状态样式应优先复用已有样式，不重复创建近似规则。

## 3. 数据和接口约定

### 账号模型

账号至少包含：

- `id`
- `displayName`
- `status`
- `productCount`
- `lastSyncAt`
- `createdAt`
- `updatedAt`

### 商品模型

商品必须包含非空字符串 `id`。常用字段包括：

- `name`
- `activityId`
- `activityName`
- `status`
- `activityPrice`
- `endsAt`
- `imageUrl`
- `updatedAt`

状态值统一使用：`active`、`suspected`、`lost`、`reviewing`。

### IPC 约定

- IPC 通道名称使用 `资源:动作` 格式，例如 `products:sync`。
- 参数必须是可结构化克隆的普通数据，不传递窗口、文件句柄或函数。
- 失败通过抛出带有用户可理解信息的 `Error` 返回。
- 新增 IPC 通道时，必须同时更新 `preload.js` 和渲染层调用代码。

## 4. 统一代码风格

### JavaScript

- 使用 CommonJS：`require` 和 `module.exports`。
- 使用 2 个空格缩进。
- 使用单引号字符串。
- 语句末尾使用分号。
- 优先使用 `const`，只有确实需要重新赋值时使用 `let`，禁止使用 `var`。
- 使用 `camelCase` 命名变量和函数，使用 `PascalCase` 命名类，使用 `UPPER_SNAKE_CASE` 命名模块级常量。
- 布尔变量使用 `is`、`has`、`can`、`should` 等前缀。
- 函数名称使用动词开头，例如 `loadAccounts`、`syncProducts`、`validateSettings`。
- 单个函数只完成一个清晰动作；出现多层嵌套或重复逻辑时拆分辅助函数。
- 不使用隐式全局变量，不修改第三方对象，不依赖未声明的运行时变量。

### 异步和错误处理

- 统一使用 `async/await`，避免新增 Promise 链式嵌套。
- 所有网络请求必须设置超时，并处理非 2xx 响应。
- 预期错误使用明确的错误类型或稳定错误码。
- 面向用户的错误信息使用中文，日志信息应包含足够的上下文。
- 不吞掉异常；如果需要降级到缓存，必须明确返回数据来源和错误状态。

### DOM 和渲染

- 用户输入写入页面时优先使用 `textContent`，避免直接拼接不可信 HTML。
- 事件监听集中放在 `app.js` 的初始化区域或清晰的功能函数中。
- 页面元素查询使用已有的 `elements` 集合或稳定选择器，避免散落重复查询。
- 弹窗、按钮和异步操作必须处理禁用状态、失败反馈和恢复状态。

### 注释

- 注释解释设计原因、外部约束或容易误解的业务规则。
- 不为显而易见的代码添加逐行注释。
- 外部接口字段映射、缓存降级和状态迁移规则必须有简短说明。

## 5. 开发和验证要求

- 新增模块前先确认职责是否已有归属，避免把逻辑继续堆入 `main.js` 或 `app.js`。
- 修改状态规则、数据模型或 IPC 接口时，必须同步更新相关测试。
- 提交前至少运行：

  ```bash
  npm test
  node --check src/main/main.js
  node --check src/renderer/app.js
  ```

- 新增外部接口适配时，必须覆盖成功响应、空数据、重复商品 ID、接口错误和超时场景。
- UI 修改需要检查空状态、加载状态、错误状态和已有账号场景。
- 不提交本地用户数据、Webhook、加签密钥、登录会话或构建产物。

## 6. 变更边界

每次变更应尽量围绕一个明确目标，避免在功能开发中顺带进行无关重构。涉及模块边界、数据模型或 IPC 协议的调整，需要在变更说明中写明影响范围、兼容方式和验证结果。
